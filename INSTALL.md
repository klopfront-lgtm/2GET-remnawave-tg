# Remnawave Bot - Инструкция по установке

## 🚀 Быстрая установка

### Требования
- Ubuntu 24.04 (рекомендуется)
- Домен,指向 на IP сервера
- Root доступ к серверу

### Шаг 1: Настройка .env файла

**ВАЖНО:** Сначала настройте `.env` файл перед запуском установщика!

```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Отредактируйте конфигурацию
nano .env
```

**Обязательные параметры для настройки:**

```bash
# Telegram Bot
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz  # Получите у @BotFather
ADMIN_IDS=123456789                               # Ваш Telegram ID (узнайте у @userinfobot)

# Домен для webhook
WEBHOOK_BASE_URL=https://bot.yourdomain.com       # Ваш домен

# База данных
POSTGRES_PASSWORD=your_secure_password             # Придумайте сложный пароль

# VPN Panel
PANEL_API_URL=http://your-panel.com/api           # URL вашей VPN панели
PANEL_API_KEY=your_panel_api_key                  # API ключ от панели
```

### Шаг 2: Запуск автоматического установщика

```bash
# Запустите установщик от root
sudo ./auto-install.sh
```

Установщик автоматически:
- Обновит систему и установит Docker
- Настроит firewall
- Получит SSL сертификат через Let's Encrypt
- Установит и настроит все сервисы
- Применит миграции базы данных

### Шаг 3: Проверка работы

После установки проверьте бота в Telegram - отправьте ему `/start`

## 📋 Полный процесс установки

### Что делает установщик:

1. **Проверка .env файла** - убеждается, что все обязательные параметры настроены
2. **Обновление системы** - устанавливает последние обновления Ubuntu
3. **Установка Docker** - устанавливает Docker и Docker Compose
4. **Настройка Firewall** - открывает порты 22, 80, 443
5. **Подготовка конфигурации** - создает необходимые директории и обновляет nginx конфиг
6. **Получение SSL сертификата** - выпускает и устанавливает Let's Encrypt сертификат
7. **Запуск сервисов** - собирает и запускает все Docker контейнеры
8. **Применение миграций** - применяет миграции базы данных

### Структура проекта после установки:

```
├── .env                    # Ваша конфигурация
├── auto-install.sh         # Основной установщик
├── get-ssl.sh             # Скрипт для ручного получения SSL
├── docker-compose.production.yml  # Docker конфигурация
├── nginx/
│   ├── nginx.conf         # Основная конфигурация Nginx
│   ├── conf.d/bot.conf    # Конфигурация для вашего домена
│   ├── ssl/               # SSL сертификаты
│   └── acme-webroot/      # Директория для ACME challenge
├── bot/                   # Код бота
├── db/                    # Миграции базы данных
└── locales/               # Локализация
```

## 🔧 Полезные команды

### Управление сервисами:

```bash
# Проверить статус контейнеров
docker ps

# Посмотреть логи бота
docker logs -f remnawave-bot

# Перезапустить бота
docker restart remnawave-bot

# Остановить все сервисы
docker compose -f docker-compose.production.yml down

# Запустить все сервисы
docker compose -f docker-compose.production.yml up -d

# Применить миграции вручную
docker exec remnawave-bot alembic upgrade head
```

### Управление SSL:

```bash
# Проверить статус SSL сертификатов
~/.acme.sh/acme.sh --list

# Обновить сертификат вручную
~/.acme.sh/acme.sh --renew -d yourdomain.com --force

# Получить новый сертификат (если что-то пошло не так)
./get-ssl.sh yourdomain.com your@email.com
```

## 🚨 Важные замечания

### Безопасность:
- **Никогда не коммитьте .env файл в Git!**
- Храните `.env` файл в безопасном месте
- Используйте сложные пароли для PostgreSQL
- Регулярно обновляйте систему

### SSL сертификаты:
- Сертификаты автоматически обновляются через acme.sh
- Сертификаты хранятся в `nginx/ssl/yourdomain.com/`
- Для обновления сертификата не требуется перезапуск сервисов

### Резервное копирование:
- База данных хранится в Docker volume `remnawave-postgres-data`
- Рекомендуется регулярно делать бэкапы:
  ```bash
  docker exec remnawave-db pg_dump -U postgres postgres > backup.sql
  ```

## 📚 Дополнительная документация

- [Руководство по развертыванию](DEPLOYMENT_GUIDE.md)
- [Руководство по миграциям](MIGRATION_GUIDE.md)
- [Руководство по обслуживанию](MAINTENANCE_GUIDE.md)
- [Руководство по безопасности](SECURITY_GUIDE.md)

## 🆘 Поддержка

Если возникли проблемы:

1. Проверьте логи: `docker logs -f remnawave-bot`
2. Убедитесь что домен указывает на IP сервера: `nslookup yourdomain.com`
3. Проверьте открытые порты: `sudo ufw status`
4. Проверьте статус контейнеров: `docker ps`

## 🔄 Обновление

Для обновления до последней версии:

```bash
# Остановите сервисы
docker compose -f docker-compose.production.yml down

# Получите последние изменения
git pull

# Пересоберите образы
docker compose -f docker-compose.production.yml build

# Запустите сервисы
docker compose -f docker-compose.production.yml up -d

# Примените миграции
docker exec remnawave-bot alembic upgrade head
```

---

**Готово! Ваш Remnawave Bot установлен и готов к работе! 🎉**