# 🎯 Установка бота на сервер - Для новичков

## Самая простая инструкция

**Время:** 15 минут  
**Сложность:** ⭐ Очень легко  
**Опыт:** Не требуется

---

## 📝 Что нужно подготовить ДО установки:

### 1. Сервер Ubuntu 24.04
- Арендуйте VPS на: [Hetzner](https://hetzner.com), [DigitalOcean](https://digitalocean.com), [VPS.ru](https://vps.ru)
- Минимум: **2 GB RAM, 20 GB диск, 1 CPU**
- Вы получите: **IP адрес** и **пароль root**

### 2. Доменное имя
- Купите домен, например: `bot.example.com`
- Где купить: [Namecheap](https://namecheap.com), [Reg.ru](https://reg.ru)
- **Настройте A-запись:** `bot.example.com` → `IP вашего сервера`
- Подождите 10-30 минут для propagation

### 3. Токен бота Telegram
- Откройте [@BotFather](https://t.me/BotFather) в Telegram
- Отправьте `/newbot`
- Следуйте инструкциям
- Скопируйте токен: `1234567890:ABCdefGHIjklMNO...`

### 4. Ваш Telegram ID
- Откройте [@userinfobot](https://t.me/userinfobot)
- Отправьте `/start`
- Скопируйте ваш ID: `123456789`

---

## 🚀 УСТАНОВКА (3 простых шага)

### ШАГ 1: Подключитесь к серверу

**Windows (PuTTY):**
1. Скачайте [PuTTY](https://www.putty.org/)
2. Запустите, введите IP сервера
3. Логин: `root`, пароль: ваш пароль

**Mac/Linux (Terminal):**
```bash
ssh root@ВАШ_IP_АДРЕС
# Введите пароль
```

---

### ШАГ 2: Скачайте и настройте бота

Скопируйте **ВСЕ команды ЦЕЛИКОМ** и вставьте в терминал:

```bash
# Обновить систему
apt update && apt upgrade -y

# Установить Git
apt install -y git

# Скачать код бота
cd /root
git clone https://github.com/YOUR_USERNAME/remnawave-tg-shop-main.git
cd remnawave-tg-shop-main

# Создать .env файл
cp .env.example .env

# Открыть для редактирования
nano .env
```

**В открывшемся редакторе найдите и ИЗМЕНИТЕ:**

```env
# Строка ~7: Вставьте токен от @BotFather
BOT_TOKEN=1234567890:ABCdefGHIjklMNO...

# Строка ~8: Вставьте ваш Telegram ID
ADMIN_IDS=123456789

# Строка ~63: Вставьте ваш домен
WEBHOOK_BASE_URL=https://bot.example.com

# Строка ~18: Придумайте сложный пароль для БД
POSTGRES_PASSWORD=ваш_сложный_пароль_123

# Строка ~38: Ваша ссылка на поддержку
SUPPORT_LINK=https://t.me/your_support_channel

# Строка ~149-150: Включите Redis
REDIS_ENABLED=True
REDIS_HOST=remnawave-redis

# Строка ~249-252: Включите Rate Limiting
RATE_LIMIT_ENABLED=True
```

**Сохраните:**
- Нажмите `Ctrl + O`, затем `Enter` (сохранить)
- Нажмите `Ctrl + X` (выйти)

---

### ШАГ 3: Запустите автоматический установщик

```bash
# Сделать скрипт исполняемым
chmod +x install.sh

# Запустить установку
./install.sh
```

**Что спросит скрипт:**

1. `Введите email`: введите ваш email для SSL сертификата
2. `Нажмите Enter`: нажмите после настройки .env (вы уже настроили)

**Установка займет 5-10 минут...**

---

## ✅ Готово! Проверьте что всё работает

### 1. Проверить контейнеры

```bash
docker ps
```

**Должны работать 5 контейнеров:**
- remnawave-nginx
- remnawave-bot
- remnawave-redis
- remnawave-db
- remnawave-certbot

### 2. Проверить логи

```bash
docker logs remnawave-bot
```

**Ищите в логах:**
- ✅ `RedisStorage: Connected to Redis`
- ✅ `Rate limiting middleware registered`
- ✅ `bot.set_webhook ... returned SUCCESS`
- ✅ `Starting bot in Webhook mode`

**Если видите ошибки** - смотрите раздел "[Что делать если не работает](#что-делать-если-не-работает)"

### 3. Проверить в Telegram

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Должен ответить!

**Если НЕ отвечает** - смотрите раздел "[Что делать если не работает](#что-делать-если-не-работает)"

---

## 🎉 Поздравляем! Бот работает!

Теперь вы можете:
- ✅ Принимать платежи (настройте YooKassa/CryptoPay в .env)
- ✅ Управлять подписками
- ✅ Использовать админ-панель
- ✅ Бот защищен от спама
- ✅ Автоматические бэкапы работают
- ✅ SSL сертификат обновляется автоматически

---

## 🛠️ Полезные команды (шпаргалка)

### Управление ботом

```bash
# Перезапустить бота
docker restart remnawave-bot

# Остановить всё
docker compose -f docker-compose.production.yml down

# Запустить всё
docker compose -f docker-compose.production.yml up -d

# Посмотреть логи (Ctrl+C для выхода)
docker logs -f remnawave-bot
```

### Обновление бота

```bash
cd /root/remnawave-tg-shop-main
git pull
docker compose -f docker-compose.production.yml up -d --build
```

### Бэкап базы данных

```bash
# Создать бэкап
docker exec remnawave-db pg_dump -U postgres postgres > backup.sql

# Восстановить из бэкапа
cat backup.sql | docker exec -i remnawave-db psql -U postgres postgres
```

---

## ⚠️ Что делать если не работает

### Проблема: Бот не отвечает в Telegram

**Решение:**
```bash
# 1. Проверить логи
docker logs --tail 50 remnawave-bot | grep ERROR

# 2. Проверить webhook
# Откройте в браузере (замените TOKEN на ваш токен):
# https://api.telegram.org/botTOKEN/getWebhookInfo

# 3. Перезапустить бота
docker restart remnawave-bot
```

### Проблема: SSL сертификат не получился

**Причины:**
- Домен еще не пропагировался (подождите 30 минут)
- A-запись указывает на неправильный IP
- Firewall блокирует порт 80

**Решение:**
```bash
# Проверить что домен указывает на ваш IP
nslookup bot.example.com
# Должен показать IP вашего сервера

# Если нет - исправьте A-запись у регистратора домена
# Подождите 30 минут и попробуйте снова:
./install.sh
```

### Проблема: "Cannot start service db"

**Решение:**
```bash
# Проверить что PostgreSQL контейнер здоров
docker ps -a | grep remnawave-db

# Посмотреть логи
docker logs remnawave-db

# Если ошибки с паролем - проверьте POSTGRES_PASSWORD в .env
```

### Проблема: "Error: connect ECONNREFUSED"

**Решение:**
```bash
# Это означает что сервис еще не запустился
# Подождите 30 секунд и попробуйте снова
sleep 30
docker restart remnawave-bot
```

---

## 📞 Нужна помощь?

1. **Прочитайте полное руководство:** [`UBUNTU_DOCKER_INSTALLATION.md`](UBUNTU_DOCKER_INSTALLATION.md)
2. **Проверьте документацию:** [`DOCUMENTATION_INDEX.md`](DOCUMENTATION_INDEX.md)
3. **Создайте Issue на GitHub** с логами

---

## 🎓 Видео-инструкция (рекомендуется)

*TODO: Добавить ссылку на видео когда будет готова*

---

## ⚡ Экспресс-установка (одна команда)

Если вы опытный пользователь:

```bash
git clone https://github.com/YOUR_USERNAME/remnawave-tg-shop-main.git && \
cd remnawave-tg-shop-main && \
cp .env.example .env && \
nano .env && \
chmod +x install.sh && \
./install.sh
```

Настройте .env, запустите скрипт, готово!

---

**Дата:** 2024-11-24  
**Версия:** 1.0  
**Автор:** Kilo Code Team  

**🎉 Удачной установки!** 🎉